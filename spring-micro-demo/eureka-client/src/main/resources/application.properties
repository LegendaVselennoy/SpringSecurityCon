spring.application.name=eureka-client

spring.profiles.active=micro

#Считывания своего файла properties
#spring.config.import=optional:congfigserver:http://localhost:8888
spring.config.import=congfigserver:http://localhost:8888
#server.port=0

# Куда будет происходить регистрация микросервисов
#eureka.client.service-url.defaultZone=http://localhost:8761/eureka

# Формат вывода логов
#logging.pattern.console=%C{1.} [%-5level] %d{HH:mm:ss} - %msg%n

#Каждый instance имел уникальный id
#eureka.instance.instance-id=${spring.application.name}:${random.uuid}