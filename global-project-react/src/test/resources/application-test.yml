spring:
  r2dbc:
    url: r2dbc:h2:mem:test;INIT=CREATE SCHEMA IF NOT EXISTS TEST
    username: sa
    password:
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        hbm2ddl:
          auto: create-drop