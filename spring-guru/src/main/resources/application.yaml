spring:
  application:
    name: spring-guru
  datasource:
    url: jdbc:postgresql://localhost:5432/my_guru
    username: postgres
    password: rom7774256
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true
        format_sql: true

logging:
  level:
    org:
      example:
        springguru: debug